import React from "react";
import FriendsItem from "./FriendsItem/FriendsItem";

const Friends = (props) => {
    // let friendsElement = props.state.friends.map(fr => <FriendsItem id={fr.id} name={fr.name}/>)

    return (
        <div>
            Friends
            {/*{friendsElement}*/}
        </div>
    )
}

export default Friends;